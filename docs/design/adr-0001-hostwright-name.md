# ADR 0001: Hostwright Name

Status: Accepted

## Decision

Use Hostwright as the public project name, `hostwright` as the CLI, `hostwrightd` as the daemon, and `hostwright.yaml` as the manifest.

## Consequences

Public-facing files use Hostwright terms. Historical source material keeps original filenames for traceability.

